<div class="col-lg-6 col-md-12">
  <div class="card">
    <div class="card-header-blue">
      <h4 class="card-title" style="font-family: Georgia, serif;">The Sun 15 Days Ephemeris</h4>
      <p class="card-category">The following table lists the ephemerides of The Sun computed for the past and next 7 days, with a 24 hours interval</p>
    </div>
    <div class="card-body table-responsive">
      <table class="table table-hover">
        <thead class="text-blue">
          <th>Date</th>
          <th>Right Ascension</th>
          <th>Declination</th>
          <th>Magnitude</th>
          <th>Apparent Diameter</th>
          <th>Constellation</th>
        </thead>
        <tbody>
          <% for (let i = 1; i < ans.length; i++) { %>
            <tr>
              <% for (let j = 0; j < ans[i].length; j++) { %>
                <td><%= ans[i][j] %></td>
              <% } %>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
</div>
